# Runtime AI

`smartsite_ai` chứa app factory FastAPI, cấu hình process, contract models/hash, Backend ingestion client và entrypoint `smartsite-ai`. Health/capability HTTP cùng lớp tích hợp contract đã chạy; camera input, PPE inference, tracking, Zone, identity và producer loop thuộc milestone sau.
